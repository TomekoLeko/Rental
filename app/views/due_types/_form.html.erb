<%= form_with(model: due_type, local: true) do |form| %>
  <% if due_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(due_type.errors.count, "error") %> prohibited this due_type from being saved:</h2>

      <ul>
        <% due_type.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field form-group">
    <%= form.label :name %>
    <%= form.text_field :name, class:"form-control" %>
  </div>
  <div class="field form-group"">
  <%= form.label :user_id %>
  <%= form.number_field :user_id, id: :due_type_user_id, value: current_user.id, class:"form-control", type: :hidden %>
</br>
</div>
</br>
  <div class="actions form-group">
    <%= form.submit class:"btn btn-success" %>
  </div>
<% end %>
